---
title: env:generate | bolt-framework
description: Run Docker containers, child processes, or even encapsulate your entire project within a VM, all under one unified API.
---

import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta title="API Reference/CLI Reference/env:generate" />

# env:generate

This command generates .env file from .env.tpl file available in each bolt service.

```bash
$ bolt env:generate --help
Usage: bolt env:generate [options]

Generates ".env" for all the services from ".env.tpl" file in each service

Options:
  -h, --help  display help for command
```
